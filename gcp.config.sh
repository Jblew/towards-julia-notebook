#!/usr/bin/env bash
PROJECT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "${PROJECT_DIR}"

PROJECT_ID="julia-notebooks"
ARTIFACT_REPOSITORY_NAME="julia-notebook-envs-repo"
GCP_LOCATION="europe-west3"
